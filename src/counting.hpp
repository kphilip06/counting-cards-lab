#pragma once

class Sort {
    public:
        static int* firstAlgorithm(const int passed[], size_t size);
        static int* secondAlgorithm(int passed[], size_t size);
        void thirdAlgorithm(int passed[], size_t size);
};
